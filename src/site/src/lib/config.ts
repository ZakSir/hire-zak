/**
 * Site configuration - easy toggles for features
 */
export const siteConfig = {
  /** Enable the floating blob background */
  enableBlobBackground: false,
  
  /** Enable parallax swoosh effects */
  enableSwooshField: false,
  
  /** Maximum number of tags to show per job */
  maxTagsPerJob: 12,
  
  /** Show the skill wheel visualization */
  showSkillWheel: false,
  
  /** Show the skill bars visualization */
  showSkillBars: true,
  
  /** Show the skill cloud visualization */
  showSkillCloud: false,
  
  /** Show the D3 career metrics section (radar, river, hero stats) */
  showCareerMetrics: true,
}
